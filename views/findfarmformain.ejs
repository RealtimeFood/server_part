<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- 위 3개의 메타 태그는 *반드시* head 태그의 처음에 와야합니다; 어떤 다른 콘텐츠들은 반드시 이 태그들 *다음에* 와야 합니다 -->
    <title>RealTime</title>

    <!-- 부트스트랩 -->
    <link rel="stylesheet" href="/css/bootstrap.css">

    <!-- IE8 에서 HTML5 요소와 미디어 쿼리를 위한 HTML5 shim 와 Respond.js -->
    <!-- WARNING: Respond.js 는 당신이 file:// 을 통해 페이지를 볼 때는 동작하지 않습니다. -->
    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
      <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->

    <script type="text/javascript">
    function search(){
        $(document).ready(function() {
            $("#keyword").keyup(function() {
                var k = $(this).val();
                $("#farm-table > tbody > tr").hide();
                var temp = $("#farm-table > tbody > tr > td:nth-child(4n+2):contains('" + k + "')");
                $(temp).parent().show();
            })
        })
      };
    </script>
  </head>

  <body>
    <body>
  		<div class = "container">
  			<div class="jumbotron">
  	  	<h1>Real Food Admin</h1>
  	  	<p>메인페이지 농장 관리 </p>
          <p><a href="/api/Farm/insertmainfarm" class="btn btn-danger" role="button">메인페이지 전용 농장 추가</a></p>
      </div>
      &nbsp
    <div class = "container">
    <h1>메인페이지 농장 관리</h1>
    <div class="col-lg-6">
      <div class="input-group">
        <span class="input-group-btn">
          <button onclick = 'search()'class="btn btn-default" type="button">농장찾기</button>
        </span>
        <input id="keyword"type="text" class="form-control" placeholder="Search for...">
      </div><!-- /input-group -->
    </div><!-- /.col-lg-6 -->
    <table class ="table" id="farm-table" type="farm-table">
      <thead>
        <tr>
          <td>번호</td>
          <td>농장 이름</td>
          <td>농장 위치</td>
          <td>농장 주인</td>
        </tr>
      </thead>
      <%if(farm.length>0){%>
        <%var i = 0;%>
        <%farm.forEach(function(item){%>
        <%i++;%>
        <tbody>
        <tr>
            <td class="number"><%=i%></td>
            <td class="farmname"><a href="http://35.200.0.166:3000/api/Farm/main/<%=item._id%>"><%=item.farmname%></a></td>
            <td class="address"><%=item.address%></td>
            <td class="farmorder"><%=item.farmorder%></td>
        </tr>
        <%})%>
        <%} else {%>
        <tr>
            <td colspan="5">게시물이 없습니다.</td>
        </tr>
<%}%>
</tbody>
          </table>
        </div>
      </div>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.2/jquery.min.js"></script>
    <!-- 모든 컴파일된 플러그인을 포함합니다 (아래), 원하지 않는다면 필요한 각각의 파일을 포함하세요 -->
    <script src="js/bootstrap.min.js"></script>
  </body>

</html>
